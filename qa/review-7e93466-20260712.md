# QA Review — 7e93466

**Date:** 2026-07-12
**Reviewer:** Grit (Grok)
**Commit:** `7e93466794cd797e6b18f51beb5a4435eb434960` (`7e93466`)

**Subject:** feat: draft toggle — fast preview mode

## Quality Gates

| gate | result |
|---|---|
| `go build ./...` | PASS |
| `go vet ./...` | PASS |
| `go test ./... -count=1` | PASS (16/16) |

All 16 `pkg/civit` tests passed; `main` and `internal/ui` have no test files. No skips.

```
ok  	github.com/m/civitui/pkg/civit	30.031s
```

## Constitution Compliance

- [x] Separation of concerns (no UI in pkg/, no HTTP in internal/ui/)
- [x] Code block rule (single command, no comments/prompts) — N/A for this code commit; no new violating command blocks introduced in implementation paths reviewed
- [x] No skipped or broken tests
- [x] JSON tags match schema

### Layer checks (detail)

| check | result |
|---|---|
| `bubbletea` / `lipgloss` / `huh` in `pkg/civit/` | clean (none) |
| `http.Client` / direct HTTP in `internal/ui/` | clean (none) |
| `Draft` tag | `json:"draft"` on `GenerationRequest.Draft` — matches API + `specs/draft-toggle.md` |
| Skipped tests (`t.Skip`, etc.) | none |

## Spec Compliance (`specs/draft-toggle.md`)

| requirement | status | notes |
|---|---|---|
| `numFormFields = 15`, `fiDraft = 14` | met | `internal/ui/ui.go` |
| `isPresetsField` includes `fiDraft` | met | |
| `isReplaceOnFocus` unchanged (width…seed) | met | draft not replace-on-focus |
| `GenerationRequest.Draft bool \`json:"draft"\`` | met | `pkg/civit/civit.go:64` |
| `draftPresets` true/false + descriptions | met | |
| field label / help text | met | help text minor wording: "Use Right Arrow to select." (spec said "Use Right Arrow.") — consistent with sibling fields |
| `NewModel` default `"false"` | met | |
| `viewConfig` Draft Mode pane | met | |
| `handleConfigKey` presetsLen + enter | met | |
| `toRequest` `Value() == "true"` | met | |

Implementation matches the architectural spec. Engine stays headless; UI only maps form state into `civit.GenerationRequest`.

## Findings

### Non-blocking

1. **Missing draft marshal assertions** — `TODO.md` marks “Update `pkg/civit/civit_test.go` or add assertions to verify that the `draft` field is marshalled correctly” complete, but `civit_test.go` was not changed. `TestGenerationRequest_Marshal` and `TestGenerationRequest_MarshalOmitEmpty` never set or assert `Draft` / `"draft"`. Existing 16 tests still pass; this is coverage debt, not a gate failure. Recommend a follow-up asserting both `"draft": true` and `"draft": false` (no `omitempty`, so `false` must serialize).

2. **TODO status text stale** — draft task still says “completed in commit pending” instead of `7e93466`.

3. **Unrelated binaries in commit** — `3409350459823.png` and `see!.png` are included with no relation to the draft toggle. Not a constitution violation; recommend stripping from history or a cleanup commit if they were accidental.

### Blocking

none

## Verdict

**PASS — ready for Bones' sign-off**

Quality gates green, constitution layers intact, `json:"draft"` correct, and the draft toggle UI/engine wiring matches `specs/draft-toggle.md`. Follow-up desirable for draft JSON unit coverage and cleanup of stray PNGs / TODO wording; none of those block sign-off.
