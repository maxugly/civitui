# QA Review — 0f4ee7a (re-QA of 5795aba blockers)

**Date:** 2026-07-13
**Reviewer:** Grit (Grok)
**Commit:** `0f4ee7aadf84cc78875156a9da4e2441c093c060` (`0f4ee7a`)

**Subject:** fix: QA blockers — upscale masking, isFieldVisible, conditional fields, clipSkip case

**Prior review:** `qa/review-5795aba-20260712.md` (FAIL — 4 blockers)

## Quality Gates

| gate | result |
|---|---|
| `go build ./...` | PASS |
| `go vet ./...` | PASS |
| `go test ./... -count=1` | PASS (17/17) |

```
?   	github.com/m/civitui	[no test files]
?   	github.com/m/civitui/internal/ui	[no test files]
ok  	github.com/m/civitui/pkg/civit	30.030s
```

All 17 `pkg/civit` tests passed. No `t.Skip` / `SkipNow` in the tree.

## Constitution Compliance

- [x] Separation of concerns (no UI in pkg/, no HTTP in internal/ui/)
- [x] Code block rule (single command, no comments/prompts) — N/A for this code commit
- [x] No skipped or broken tests
- [x] JSON tags match schema (unchanged from 5795aba review; tier-2 tags still correct)
- [x] **All numeric fields have keystroke-level input masking** (Validate callback + rollback + errMsg flash + justFocused + isReplaceOnFocus)

### Layer checks (detail)

| check | result |
|---|---|
| `bubbletea` / `lipgloss` / `huh` in `pkg/civit/` | clean (none) |
| `http.Client` / direct HTTP in `internal/ui/` | clean (none) |
| Skipped tests | none |
| JSON tags | unchanged; `baseModel`, `clipSkip`, `upscaleWidth/Height`, `fluxUltraRaw`, etc. still match |

### Input masking checklist (upscale re-check)

| field | keystroke Validate | min location | rollback + errMsg | justFocused / isReplaceOnFocus | usable? |
|---|---|---|---|---|---|
| `fiUpscaleWidth` | regex + **max only** (`val > 3840`) | `validateNumericFields` 320–3840 | yes (shared block) | yes | **YES** |
| `fiUpscaleHeight` | regex + **max only** (`val > 3840`) | `validateNumericFields` 320–3840 | yes | yes | **YES** |

Matches width/height pattern: progressive entry of `1024` → `"1"`, `"10"`, `"102"`, `"1024"` all pass keystroke Validate; min enforced only on submit.

## Blocker re-verification

### 1. Upscale Validate — min-320 removed from keystroke level — **FIXED**

| site | status | evidence |
|---|---|---|
| keystroke `fiUpscaleWidth` | max-only | `internal/ui/ui.go:688–689` — `if val > 3840` only |
| keystroke `fiUpscaleHeight` | max-only | `:704–705` — `if val > 3840` only |
| submit min retained | yes | `validateNumericFields` `:896` / `:904` — `val < 320 \|\| val > 3840` |

No residual `val < 320` in either Upscale `Validate` callback. Typing `1024` is possible.

### 2. `isFieldVisible` wired into view + tab nav — **FIXED**

| call site | pattern | evidence |
|---|---|---|
| `viewConfig` form loop | `if !m.isFieldVisible(i) { continue }` | `:1125–1129` |
| tab / down | `for { activeInput = (active+1)%N; if isFieldVisible { break } }` | `:1488–1493` |
| shift+tab / up | same skip-until-visible, reverse direction | `:1501–1506` |

Dead-code issue from 5795aba is resolved. Hidden fields (denoise always; clipSkip for Flux/ZImage; fluxUltraRaw unless Ultra URN) neither render nor receive focus via tab cycle.

### 3. Conditional fields in `toRequest` via `nilIf` — **FIXED**

| field | wiring | evidence |
|---|---|---|
| `Denoise` | `nilIf(!isFieldVisible(fiDenoise), …)` | `:753` |
| `ClipSkip` | `nilIf(!isFieldVisible(fiClipSkip), …)` | `:754` |
| `FluxUltraRaw` | `nilIf(!isFieldVisible(fiFluxUltraRaw), …)` | `:758` |
| `nilIf` helper | generic; returns nil when cond true | `:795–802` |

Default Flux model no longer leaks `clipSkip: 2` onto the wire. Non-Ultra flux mode no longer leaks `fluxUltraRaw: false`. Upscale (always-visible optional) and Experimental (always-visible bool) correctly remain unconditional.

### 4. clipSkip case-insensitive model check — **FIXED**

```
model := strings.ToLower(m.inputs[fiModel].Value())
return !strings.Contains(model, "flux") && !strings.Contains(model, "zimage")
```

Evidence: `internal/ui/ui.go:464–465`. Matches scheduler-side `"zimage"` convention (`:1599` also uses `ToLower`). `zImage` / `ZImage` / `zimage` all hide clipSkip.

## Findings

### Blocking

none

### Non-blocking (carried from 5795aba; not regressions)

1. **No tier-2 marshal tests** — still no assertions for `sourceImage` / `denoise` / `clipSkip` / `upscale*` / `experimental` / `fluxUltraRaw` present/omit in `civit_test.go`. Gates green; coverage debt remains.
2. **Denoise img2img plumbing incomplete** — `isFieldVisible(fiDenoise)` still hardcodes `return false` (`:462`). Acceptable now that visibility is wired: denoise is actually hidden in view/nav and suppressed in `toRequest`.
3. **Advanced Settings separator missing** — `specs/experimental.md` §3d still unmet.
4. **Denoise default value** empty vs spec `"0.4"` (field hidden anyway).
5. **`validateNumericFields` does not skip hidden fields** — clipSkip/denoise still range-checked on submit even when invisible. Harmless with current defaults (empty denoise; default clipSkip `2` in range). Prefer gating with `isFieldVisible` later for consistency.

## Verdict

**PASS — ready for Bones' sign-off**

All four blockers from `review-5795aba` are fixed in `0f4ee7a`: upscale keystroke min removed (min retained on submit), `isFieldVisible` applied in view + tab/shift-tab skip loops, `nilIf` suppresses conditional tier-2 pointers in `toRequest`, and clipSkip model detection is case-insensitive via `ToLower` + `"zimage"`. Quality gates green; constitution checks clean.
