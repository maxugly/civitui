# QA Review — 37ad029

**Date:** 2026-07-13
**Reviewer:** Grit (Grok)
**Commit:** `37ad029dd85f4b885476820b78115664e7ab8aa6` (`37ad029`)

**Subject:** fix: edge case audit — 3 blockers + 2 minors from Bones' review

**Spec:** `specs/dynamic-visibility-edge-cases.md`

## Quality Gates

| gate | result |
|---|---|
| `go build ./...` | PASS |
| `go vet ./...` | PASS |
| `go test ./... -count=1` | PASS (17/17) |

All 17 `pkg/civit` tests passed; `main` and `internal/ui` have no test files. No `t.Skip` / `SkipNow` / `Skipf`.

```
ok  	github.com/m/civitui/pkg/civit	30.032s
```

## Constitution Compliance

- [x] Separation of concerns (no UI in pkg/, no HTTP in internal/ui/)
- [x] Code block rule — N/A (no new operator-facing command blocks in this commit)
- [x] No skipped or broken tests
- [x] JSON tags match schema — N/A (no struct/tag changes in this commit)
- [x] **All numeric fields have keystroke-level input masking** — `fiCFGScale` still has Validate + rollback + errMsg + justFocused + isReplaceOnFocus

### Layer checks (detail)

| check | result |
|---|---|
| `bubbletea` / `lipgloss` / `huh` in `pkg/civit/` source | clean (mentions only in AGENTS.md prose) |
| `http.Client` / `net/http` in `internal/ui/` source | clean (mentions only in AGENTS.md prose) |
| Skipped tests | none |

### Input masking — `fiCFGScale` (re-verified)

| checklist item | status | location |
|---|---|---|
| `Validate` callback | present | `ui.go` ~771–791 — regex + range via `maxCFGScale(ecosystemForAIR(...))` |
| rollback on `Err` | present | `ui.go` ~1761–1771 — restores `oldValue` / `oldPos` |
| `errMsg` flash | present | `ui.go` ~1768 |
| `justFocused` gate | present | set on tab/refocus; consumed on first printable key ~1751–1755 |
| `isReplaceOnFocus` | present | `fiCFGScale` in `fiWidth…fiSeed` range (`ui.go` ~98–101) |

Keystroke and submit-time CFG caps now share the same ecosystem resolver (`ecosystemForAIR` / `currentEcosystem` → `maxCFGScale`).

## Spec Compliance (`specs/dynamic-visibility-edge-cases.md`)

| # | requirement | status | notes |
|---|---|---|---|
| 1 | `ecosystemForAIR()` detects `flux`→flux1, `flux2`/`klein`→flux2, `zimage`→zimage, etc. | **met** | Preset match first; then `flux2`/`klein` → `flux2`; bare `flux` → `flux1` (order-safe vs flux2); `zimage`; `sdxl`/`pony`/`illustrious`; `sd15`/`sd1`; default `sdxl`. `ui.go` ~627–651 |
| 2 | `adjustFieldsForEcosystem()` resets stale sampler/scheduler after model switch | **met** | Validates current values against `filteredSamplerPresets` / `filteredSchedulerPresets`; resets to first valid. Invoked on model preset Enter. `ui.go` ~538–587, ~1626 |
| 3 | Clears `fluxMode` / `fluxUltraRaw` when leaving Flux | **met** | `if eco != "flux1"` → `fluxMode=""`, `fluxUltraRaw="false"`. `ui.go` ~589–593 |
| 4 | `refocusIfHidden()` moves focus when active field becomes hidden | **met** | Blur → scan visible → Focus + `justFocused`. Called after model preset apply. `ui.go` ~598–615, ~1627 |
| 5 | Tab nav loops have safety exits (`startInput` check) | **met** | Both `tab`/`down` and `shift+tab`/`up` break when `activeInput == startInput`. `ui.go` ~1691–1722 |
| 6 | `fluxUltraRaw` requires `ecosystem=="flux1"` in `isFieldVisible` | **met** | `ui.go` ~519–520 |
| 7 | cfgScale `Validate` uses `ecosystemForAIR()` not raw string matching | **met** | Replaced `strings.Contains(modelVal, "flux")` with `maxCFGScale(ecosystemForAIR(...))`. `ui.go` ~786 |
| 8 | `currentEcosystem()` delegates to `ecosystemForAIR()` | **met** | `ui.go` ~532–534 |

Findings from the audit that were **out of scope** for this commit (commit message claims 3 blockers + 2 minors only):

| audit # | item | status |
|---|---|---|
| 5 | sampler preset pane inquiry | N/A — no change required |
| 7 | toRequest omitempty cleanup | N/A — confirmed correct behavior, no change |
| 8 | `NewModel` default sampler `"Euler a"` vs initial ecosystem | **not fixed** — residual minor; default model is still Flux1 so sampler field stays hidden at boot |

## Findings

### Non-blocking

1. **TODO.md not marked complete** — Commit added section `Active Tasks — Dynamic Visibility Blockers` with three tasks still `[ ]`, but the code implements all three. Tom should flip them to `[x]` (and move to Completed) after this QA pass.

2. **TODO.md formatting glitch** — Line merge: `` `go test ./... -count=1`.### Bugfix `` missing newline between the quality-gates bullet and `### Bugfix`. Pre-existing-looking corruption introduced/adjacent in this commit’s TODO edit.

3. **Finding 8 residual** — `NewModel` still seeds `fiSampler` with `"Euler a"` (`ui.go` ~703). Harmless while default model is Flux1 (sampler hidden / `toRequest` omits when not visible). Would bite if default model became Flux2/ZImage. Spec minor; not claimed in this commit.

4. **Comment vs reality on custom AIR entry** — `adjustFieldsForEcosystem` comment says it runs on “preset selection or custom AIR entry,” but `fiModel` is presets-only (typing eaten). Adjustment runs only on preset Enter. `ecosystemForAIR` still correctly classifies custom AIR strings for CFG/visibility if the model value ever holds one.

5. **Re-entering Flux1 leaves empty `fluxMode`** — Leaving Flux clears `fluxMode` to `""` (correct for leak prevention). Returning to Flux1 does not restore Standard URN; field is visible with empty value until user re-selects. Spec only required clear-on-leave. Optional polish.

6. **No UI unit tests for ecosystem helpers** — `internal/ui` still has zero test files. Logic is pure and testable (`ecosystemForAIR`, visibility predicates). Coverage debt, not a gate failure.

### Blocking

none

## Verdict

**PASS — ready for Bones' sign-off**

Quality gates green (build / vet / 17 tests). Constitution layers intact. All eight claimed edge-case fixes from `specs/dynamic-visibility-edge-cases.md` are present and correctly wired. cfgScale input masking remains complete and now shares ecosystem resolution with submit-time validation. Non-blocking follow-ups: mark Dynamic Visibility TODO items done, fix TODO newline glitch, optional Finding 8 / re-enter Flux polish.
